# sakila-payment-api-spec

This module contains the `OpenAPI v2.0` specification for the `sakila-payment-service`. 
This specification is used to generate the server code in the `sakila-payment-service`,
and the client code in the `sakila-frontend` module.

This module must be installed before the `sakila-payment-service`, `sakila-frontend`
modules are built.

## Install

Install this module to your local m2 repository:

`mvn -pl sakila-payment-api-spec am clean install`
